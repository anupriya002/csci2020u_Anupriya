//The Fruit interface that is implemented by all fruit classes (apple, banana, grapes, mango, orange)
public interface Fruit {
    //implemented by all subclasses, used to generate
    // appropriate output based on the specified class
    void describeFruit(String name, String color);
}

